server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: web-demo
  boot:
    admin:
      client:
        url: http://localhost:8788

logging:
  level:
    com.myhexin: debug
    root: warn